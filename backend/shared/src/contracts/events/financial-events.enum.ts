export enum FinancialExchangeName {
  TRANSACTION_CREATED = "financial.transactions.created.exchange",
  TRANSACTION_UPDATED = "financial.transactions.updated.exchange",
  TRANSACTION_DELETED = "financial.transactions.deleted.exchange",
}

export enum FinancialRoutingKey {
  TRANSACTION_CREATED = "transaction.created",
  TRANSACTION_UPDATED = "transaction.updated",
  TRANSACTION_DELETED = "transaction.deleted",
}
